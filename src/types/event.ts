export type AppEvent = {
  id: number;
  title: string;
  body: string;
  thumbnailUrl: string;
  url: string;
};

export type Organizer = {
  id: number;
  name: string;
  email: string;
  avatarUrl: string;
};
