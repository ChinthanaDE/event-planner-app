import React from 'react';
import { View, Text } from 'react-native';

const EventContainer = () => {
  return (
    <View style={{ padding: 20 }}>
      <Text>Event Container</Text>
    </View>
  );
};

export default EventContainer;
